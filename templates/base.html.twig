<!DOCTYPE html>
<html>
    <head>
        {% block meta %}
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width; initial-scale=1.0; maximum-scale=1.0;" />
        <meta name="turbo-cache-control" content="no-cache">
        <meta name="turbo-visit-control" content="reload">
        <meta name="csrf-token" content="{{ csrf_token('authenticate') }}">
        {% endblock %}
        
        <title>{% block title %}BenSops - Votre Expert en Solutions Juridiques{% endblock %}</title>
        
        <!-- Favicon -->
        <link rel="icon" href="/favicon.ico" type="image/x-icon">
        
        {% block javascripts_head %}
            {{ encore_entry_script_tags('app') }}
            {{ encore_entry_script_tags('calendar') }}
            <script>
                window.chatSessionUrl = '{{ path('chat_session') }}';
                window.chatPrivateUrl = '{{ path('chat_private', {'roomId': 0}) }}';
                window.isAdmin = {% if is_granted('ROLE_ADMIN') %}true{% else %}false{% endif %};
            </script>
        {% endblock %}

        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
            {{ encore_entry_link_tags('calendar') }}
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Open+Sans+Condensed:wght@300;700&display=swap" rel="stylesheet">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
        {% endblock %}
        
    </head>
    <body>
    
    {# Header - simple include, no controller needed #}
    {% include 'base/header.html.twig' %}
    {% include 'components/section-presentation.html.twig' %}

    

    {# Flash messages - positioned after header #}
    <div id="flash-messages-zone" style="position: fixed; top: 80px; left: 50%; transform: translateX(-50%); z-index: 9999; width: 90%; max-width: 500px; pointer-events: none;">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label == 'success' ? 'success' : 'warning' }} alert-dismissible fade show text-center mb-2" role="alert" style="pointer-events: all;">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {% endfor %}
        {% endfor %}
    </div>

    {# Main content #}
    <main>
    {% block body %}
    {# Page content goes here #}
    {% endblock %}
    </main>
    
    {# Widget de chat moderne #}
    <div id="chat-widget-container" class="chat-widget-modern">
        <div class="chat-widget-badge">
            <span class="widget-text">Contactez-nous</span>
            <div class="widget-buttons">
                <button id="open-chat-modal" class="btn-chat" title="Chat en ligne">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                </button>
                <a href="https://wa.me/33744644894" class="btn-whatsapp" title="WhatsApp" target="_blank" rel="noopener noreferrer">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
                        <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.890-5.335 11.893-11.893A11.821 11.821 0 0020.465 3.516"/>
                    </svg>
                </a>
            </div>
        </div>
    </div>

    <style>
        .chat-widget-modern {
            position: fixed;
            bottom: 20px;
            right: 20px;
            z-index: 9999;
        }

        .chat-widget-badge {
            background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
            border-radius: 25px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.15);
            padding: 12px 20px;
            display: flex;
            align-items: center;
            gap: 15px;
            border: 1px solid rgba(0,0,0,0.1);
            max-width: calc(100vw - 40px);
            transition: all 0.3s ease;
        }

        .chat-widget-badge:hover {
            box-shadow: 0 6px 25px rgba(0,0,0,0.2);
            transform: translateY(-2px);
        }

        .widget-text {
            font-weight: 600;
            color: #333;
            font-size: 14px;
            white-space: nowrap;
        }

        .widget-buttons {
            display: flex;
            gap: 10px;
        }

        .btn-chat, .btn-whatsapp {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            border: none;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
        }

        .btn-chat {
            background: #007bff;
            color: white;
        }

        .btn-chat:hover {
            background: #0056b3;
            transform: scale(1.1);
        }

        .btn-whatsapp {
            background: #25D366;
            color: white;
        }

        .btn-whatsapp:hover {
            background: #128C7E;
            transform: scale(1.1);
            color: white;
        }

        /* Responsive pour écrans ≤ 575px */
        @media (max-width: 575px) {
            .chat-widget-modern {
                bottom: 15px;
                right: 15px;
            }

            .chat-widget-badge {
                max-width: calc(33.33vw); /* Un tiers de l'écran */
                padding: 10px 15px;
                gap: 10px;
            }

            .widget-text {
                font-size: 12px;
                display: none; /* Masquer le texte sur très petits écrans pour économiser l'espace */
            }

            .widget-buttons {
                gap: 8px;
            }

            .btn-chat, .btn-whatsapp {
                width: 40px;
                height: 40px;
            }
        }

        /* Pour écrans très petits, affichage minimal */
        @media (max-width: 480px) {
            .chat-widget-badge {
                padding: 8px 12px;
                max-width: calc(30vw);
            }

            .btn-chat, .btn-whatsapp {
                width: 35px;
                height: 35px;
            }

            .btn-chat svg, .btn-whatsapp svg {
                width: 16px;
                height: 16px;
            }
        }

        /* Desktop - remonter le badge davantage pour éviter le footer - DOIT ÊTRE EN DERNIER */
        @media (min-width: 992px) {
            #chat-widget-container.chat-widget-modern {
                bottom: calc(20px + 5rem) !important;
            }
        }
    </style>

    {# Modal de chat #}
    <div id="chat-modal-overlay" style="display: none; position: fixed; z-index: 10000; left: 0; top: 0; width: 100vw; height: 100vh; background: rgba(0,0,0,0.3);">
        <div id="chat-modal" style="background: #fff; border-radius: 15px; max-width: 800px; margin: 60px auto; padding: 24px; position: relative;">
            <button id="close-chat-modal" style="position: absolute; top: 12px; right: 12px; background: none; border: none; font-size: 1.5em;">&times;</button>
            <div id="chat-modal-content"></div>
            <button id="open-full-chat" style="margin-top: 10px; float: right;">Ouvrir en grand</button>
        </div>
    </div>
    
    {# Footer #}
        {% include "base/footer.html.twig" %}
    
    </body>
</html>